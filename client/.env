# this env var is used to set the base path for the router in the client app.
# It should match the path where the server is serving the client app
# e.g. if the server serves the client at http://localhost:3001/dailylist, then this should be set to /dailylist
ROUTER_BASE_PATH=/dailylist