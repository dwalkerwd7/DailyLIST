# change this to match the base path where the server is serving the client app
# it's not required if the server is serving the client at the root path (e.g. http://{domain_name}/)
# only the initial '/' is required 
BASE_PATH=/dailylist
VERSION=0.8.1
